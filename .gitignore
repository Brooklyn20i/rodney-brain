# Repo-level local and secret-bearing artefacts
.vercel/
node_modules/
**/node_modules/

# Supabase CLI local temp state can include project refs / connection strings.
Cadence/supabase/.temp/
**/.temp/

# Playwright run scratch output (screenshots/traces on failure)
**/test-results/

# Native iOS public-client build values. Never commit service credentials.
Cadence/Config/Secrets.xcconfig

# Native build products and local SwiftPM/Xcode state.
**/.build/
**/.swiftpm/
**/DerivedData/
