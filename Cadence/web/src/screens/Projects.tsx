// The Projects screen lives in ./projects/ — this shim keeps App.tsx's lazy
// import path (and deep-link initialSelectedId prop) stable across the
// redesign.
export { Projects } from './projects';
